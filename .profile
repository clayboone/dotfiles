# LightDM doesn't read ~/.zshrc, but it does read this file. So set TERMINAL
# here so that i3-sensible-terminal has access to the variable when i3 is
# launched by LightDM.
export TERMINAL="termite"
